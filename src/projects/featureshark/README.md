# FeatureShark

Project-specific compositions belong in this folder.

Use shared motion and UI primitives from `../../components` instead of duplicating them here. Export composition registrations from `index.ts`, then add those exports to `src/Root.tsx` when this project is ready for Studio.
